{
  "name": "@datazar/mongo",
  "private": true,
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "devDependencies": {
    "@types/rosie": "^0.0.45",
    "chance": "^1.1.11",
    "dotenv": "^16.4.5",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc",
    "build:watch": "pnpm run build --watch",
    "dev": "tsc --watch",
    "dev:seed": "node --no-warnings=ExperimentalWarning --loader ts-node/esm -r dotenv/config ./src/test/scripts/seed-mongo.ts dotenv_config_path=.dev.env"
  },
  "dependencies": {
    "@datazar/base-processor": "workspace:^",
    "@datazar/anonymizers": "workspace:^",
    "@datazar/common": "workspace:^",
    "chalk": "^5.3.0",
    "mongodb": "^4.3.0"
  }
}